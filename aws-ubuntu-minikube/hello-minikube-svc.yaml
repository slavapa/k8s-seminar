apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-minikube
  name: hello-minikube
spec:
  type: NodePort
  selector:
    app: cheddar-cheese
  ports:
  - nodePort: 31239
    port: 8080
    protocol: TCP
    targetPort: 8080
